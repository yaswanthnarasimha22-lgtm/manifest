---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tetris1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tetris1
  template:
    metadata:
      labels:
        app: tetris1
    spec:
      containers:
      - name: tetris1
        image: shaikmustafa/mytetrisgame:v1
        ports:
        - containerPort: 3000   # Use port 3000

---
apiVersion: v1
kind: Service
metadata:
  name: tetris-service
spec:
  selector:
    app: tetris
  ports:
  - protocol: TCP
    port: 80                 # Expose port 80
    targetPort: 3000
  type: LoadBalancer
